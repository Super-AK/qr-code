qr-generator/
│
├── index.php          ← Hauptdatei (kann auch .html sein, aber wir nutzen .php für Erweiterbarkeit)
├── js/
│   └── qrcode.min.js  ← QR-Code-Bibliothek
└── css/
    └── style.css      ← Eigenes Styling (optional)